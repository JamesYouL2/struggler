{
  "_what": "Weight A/Bs for .github/workflows/experiments.yml. One entry per arm; every entry runs as its own job, in parallel, on its own runner.",
  "_schema": {
    "slug": "filename-safe id; names the artifact and the report",
    "weights": "object merged into StrategicWeights, e.g. {\"scoring_final\": 3.0}",
    "seeds": "the verdict sample, as lo-hi",
    "held": "the held-out sample, disjoint from seeds -- required, because --decide is gated on a held sample existing and passing --decide alone does nothing",
    "title": "one line for the generated note",
    "context": "why this arm, what it should show, and what it cannot -- the runner will not interpret the result for you"
  },
  "_sizing": "128 seeds by default and 256 the ceiling, matching scripts/lib/queue_common.sh. The reason for 128 is early stopping: ACCEPTANCE min_games is 150 pooled, so an 80-seed run is 160 games and --decide has ten games of slack. Measured 2026-09-12: an 80-seed run saved 4%, a 128-seed run 16%. The reason for 256 as a ceiling is that half-width falls as 1/sqrt(n) and 512 would buy +/-0.028 for four and a half hours.",
  "_burst": "The local queue existed because one machine runs one thing at a time; a runner per arm removes that. Prefer MORE ARMS AT FULL SIZE over fewer underpowered ones -- several 2026-09-12 experiments read +/-0.06 to +/-0.075 and could not settle what they were asked, purely because the box was the constraint.",
  "experiments": [
    {
      "slug": "region-par-rerun",
      "weights": {
        "region": 1.0
      },
      "seeds": "9700-9827",
      "held": "9828-9891",
      "title": "region at par (1.0 vs 1.3), full-size re-run on current main",
      "context": "Rebuild step 2 prices factor-1 value with region at par (2026-09-14-value-rebuild-plan). The 2026-09-14 arm read pooled ~0.514 (0.523 over 128 seeds, 0.496 over 64 held) on the superseded pre-merge model: a lean, not a verdict. Re-run at full 128+64 on current main (italy default, rival urgency, retention merged). Shows whether par is free; cannot ship calibration by itself, the rebuild re-derives factor 1 anyway."
    },
    {
      "slug": "access-contested-off-rerun",
      "weights": {
        "access_contested": 0.0
      },
      "seeds": "9892-10019",
      "held": "10020-10083",
      "title": "contested reach at zero (0.0 vs 0.25), isolated on current main",
      "context": "Isolates the contested-zero piece of the removals bundle without the five-bucket change. The 2026-09-14 arm read pooled 0.512 (0.498 over 128, 0.539 over 64 held) pre-italy: dead heat leaning up. Re-run on the stronger base; informs the five-bucket bisect whichever way it lands. Measures only the weight piece: the flat holding bonus is code, not a weight, and is not covered by this arm."
    },
    {
      "slug": "coup-discount-flat",
      "weights": {
        "coup_discount": 1.0
      },
      "seeds": "10084-10211",
      "held": "10212-10275",
      "title": "no coup discount (1.0 vs 0.9), isolated ablation at full size",
      "context": "The 2026-09-09 audit proposed testing coup_discount=1 as an isolated ablation; the 2026-09-12 arms came back not-measurably-worse at +/-0.06 to +/-0.075, too wide to act on. Full 128+64 re-run on current main. A pass means the discount is free, not that it is useless; a fail prices the discount. Cannot distinguish a useless discount from coups mispriced elsewhere."
    }
  ]
}
