# Handoff, 2026-09-23: what landed tonight, the arm in flight, and what to pass on

## 1. What landed (all measured, all recorded)

| what | reading | record |
| --- | --- | --- |
| the hold-option grid | nothing above 0 at 0.25/0.5/1.0; `hold_option` stays 0 | [grid](2026-09-22-the-hold-option-grid.md) |
| the refit at current main | **lost**: 0.483 [0.468, 0.498]; shipped weights stand at 2.795 | [refit](2026-09-22-the-refit-at-current-main.md) |
| the potential verdict | +0.021 [-0.000, +0.042]: rule says stop; VP-rebuild **complete** | [verdict](2026-09-23-the-potential-verdict.md) |
| the assignment planner | designed + built (solver, price table, ranking lead) behind `hand_assignment` at 0 | [design](2026-09-22-assignment-planner-design.md), [tail reads](2026-09-23-assignment-planner-tail-reads.md) |
| `ci-watchdog`, the pre-push hook, `dispatch-reading`/`write-note`/`handoff` skills | proven by smoke, hooked, mirrored | [harness](2026-09-22-the-pi-harness-and-what-to-ask-of-it.md) |

PRs: **#42 and #44 merged** (the two verdicts' records); **#43 open**
(skills/watch-file convention -- your merge click). Branch
`feat/hand-planner-assignment` @ `b85afa7` is green (parity oracle
bit-identical at weight 0; hook lane 1178 tests in 0:57).

## 2. What is in flight

**Run 35814771005** -- `hand-assignment-{base,on}`, block 78000-79023 +
held 89000-89127 vs `bc5ef93`, paired, ~2 h. Its watch file is armed
(`logs/watches/35814771005`) and the rule is quoted there and in the
arm's `context`: **paired lower bound above 0 -> the allocation beats
per-card pricing, the gate opens (gate first, default flips, then step 4
asks the DP question); at or below 0 -> the gate stays shut.** The
tail-reads note holds the before-the-number evidence (USSR 0.25 tail
6/32 -> 3/32) -- written before the number on purpose.

## 3. Open, in order

1. Apply the arm's rule when it lands. Nothing else decides it.
2. Step 4 only if it clears: does the planner reproduce the DP's risks
   on the corpus? Only then delete `DefconPlanner`.
3. Yours: merge #43; the fate of the 23 unmerged branches (sweep list in
   the 2026-09-22 status note); `EXPERT_ASKS.md` pricing.

## 4. What to pass to a weaker model

This session's mistakes, ordered by damage -- each already mechanised or
recorded where it happened:

1. **Pushing red twice.** Local subsets missed `test_types` and
   `test_scale_discipline` (both < 1 s). Now mechanised: the pre-push
   hook (`git config core.hooksPath hooks`), everything but the corpus
   and the slow five. Run it, or run the whole thing.
2. **The wrong-tree pytest.** `uv run pytest` in a worktree fell back to
   the PATH's pytest and tested the MAIN tree -- impossible failures
   (`hold_option` where none exists) were the tell. Use
   `./.venv/bin/python -m pytest` after `uv sync --extra test`, and
   print `policy.__file__` first. Read an impossible failure as
   wrong-tree evidence.
3. **Anchoring an edit on a "structure tail" without confirming the
   container.** The arm pair landed in `_retired.arms`; a README bullet
   replaced instead of prepending. Verify which list a tail closes, and
   read the file back after an edit.
4. **Stub-garbage in two drafts** (`before if False else 0.0`; an
   `opens = ... if False else ...`). Both caught re-reading my own diff
   before commit. A draft is not a change.
5. **The table that carried flags into arithmetic** -- caught by the
   sentinel refusing (`_refuse`), which is the system working: price
   tables bound with `priced()` at the boundary, and a played-game smoke
   finds what fixtures cannot. Smoke before a real run rides on it --
   three of tonight's defects were smoke finds.

Standing: notes are per-agent (`docs/notes/pi/` is this tree; shared
generators take `NOTES_DIR`); `AGENTS.md` == `CLAUDE.md` byte-for-byte;
the skill mirrors and the note index are suite-gated
(`tests/test_agent_files.py`).
