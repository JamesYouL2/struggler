# Sixteen of CI's gate games, replayed on this machine

Generated by `scripts/overnight3.sh` step 3. Seeds are deterministic, so a
game CI played should come out identical here; that is what lets a CI
verdict stand in for a local one. Codex's audit F3 adds that the *prefix*
an early-stopped run keeps depends on completion order, which is not
deterministic -- this checks the games themselves, not the prefix.
Seeds 4000-4003 and 5000-5003, both seats, against CI run 34731449614.

- src/struggler at launch is identical to the CI candidate 93a78d3

```text
full-vs-base: 8 of 8 planned games finished
  reference played 84; 8 in both; identical (result, turn, VP, reason) on 8
  new:       score 0.5 +/-0.0 over 4 seeds
  reference: score 0.488 +/-0.052 over 42 seeds
full-vs-held: 8 of 8 planned games finished
  reference played 84; 8 in both; identical (result, turn, VP, reason) on 8
  new:       score 0.625 +/-0.206 over 4 seeds
  reference: score 0.512 +/-0.071 over 42 seeds
```

## What this does not say

Sixteen identical games do not prove every game replays; one that differs
would be a finding in its own right and is listed rather than explained.
The scores here are over four seeds and mean nothing.
