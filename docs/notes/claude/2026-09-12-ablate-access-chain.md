# Ablating access_chain alone, 128 seeds

The bisected access ablation. access_chain gates the entire two-hop walk inside access(): the second loop's only contribution is w.access_chain * contested * importance, so zeroing it silences that traversal while access=1.5 and the whole first-hop term stay intact -- countries remain distinguishable, which is exactly what the retired whole-family ablation could not promise. Counted over the board the chain walk is 92% of the traversal access() can do (224 first-hop iterations against 670 second-hop and 2056 third-hop scans), and access is the largest term in the profile at ~11% of a game. If this accepts the payoff is structural: the loop can be guarded away, VALUE_RADIUS falls from 3 to 2, dependents() shrinks per placement, and the two-hops-out read behind two of the seven caching defects stops existing.

## What was measured

- **score 0.468 +/-0.079** over 55 seeds (109 games)
- one-sided 95% upper bound **0.547** (acceptance needs 0.500)
- **reaches parity** on the gate's own rule
- the interval straddles 0.500: this is **not a measurable difference in either direction**, which is a bound on the effect, not evidence that it is zero
- nuclear losses: 10
- US win rate: 0.578
- mean ending turn: 8.06
- reached the Late War: 0.67
- mean game seconds: 156.8
- endings: {'final_vp': 45, 'vp': 29, 'defcon_1': 22, 'wargames': 9, 'europe_control': 4}

## What this does not say

Generated by `scripts/report_note.py` from the raw report, which is
under gitignored `logs/`. It records numbers only. Whether the result
means the change should land is a judgement nobody made at the time
it was written (2026-09-12), and the interval above is usually wider
than the effect being looked for.
